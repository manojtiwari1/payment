spring.application.name=Payment
spring.profiles.active=dev

# i18n message bundle & resolves ResponseCode keys (DUPLICATE, NOT_FOUND, ...) in messages/local.properties
spring.messages.basename=messages/local
spring.messages.encoding=UTF-8
